---
description: Create a new personalized course on a topic via guided research, interview, and curriculum design.
---

Use the `create-course` skill from the `tutor` plugin. The user's topic prompt is in `$ARGUMENTS`. If `$ARGUMENTS` is empty, ask the user for the topic before invoking the skill.

$ARGUMENTS
