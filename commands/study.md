---
description: Study an existing tutor course. Resumes the most recently studied course by default, or loads a specific course if its slug is passed as an argument.
---

Use the `study-course` skill from the `tutor` plugin. The optional course slug is in `$ARGUMENTS`. If `$ARGUMENTS` is empty, load the most recently studied course from `~/.claude/learning/index.json`.

$ARGUMENTS
